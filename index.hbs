{{!-- Put this into default.hbs --}}
{{!< default}}

{{!-- Navbar partial --}}
{{> "navbar"}}
{{> "search"}}

<main id="i-main">
    
{{> "index-header"}}

{{#match posts.length ">" 6}}
    <div class="i-home-section">
        <div class="i-card-grid">
            {{#foreach posts from="7" to="12"}}
                {{> "line-card"}}
            {{/foreach}}
        </div>
    </div>
{{/match}}

</main>

{{pagination}}

{{> "footer"}}
