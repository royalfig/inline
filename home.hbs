{{!-- Put this into default.hbs --}}
{{!< default}}

{{!-- Navbar partial --}}
{{> "navbar"}}
{{> "search"}}

<main id="main">
    <section class="i-hero i-home-section">
        
                <div class="i-hero-grid-flex">
                    <div class="i-hero-grid">
                        <div class="i-hero-text">
                            <p>Hello, I'm Ryan.</p>
                            <p>I'm a Developer Experience Engineer at the open source, publishing platform <a href="https://ghost.org" target="_blank" rel="noopener">Ghost</a></p>
                            <p>I love building websites and apps with <span class="i-js">JavaScript</span> and sharing what I learn</p
                            >
                            <p><a href="#/portal/subscribe">Subscribe to my newsletter</a> so you don't fall behind and get held back or just check out some of my favorite writing &rarr;</p>
                        </div>
                        <img class="i-hero-img" src="{{img_url @site.cover_image}}" alt="">
                    </div>
                    <div class="i-hero-grid-cards">
                        {{#get "posts" filter="featured:true" include="tags" limit="3" as |featured|}}
                        {{#foreach featured}}
                            {{> "line-card"}}
                        {{/foreach}}
                        {{/get}}
                    </div>
                </div>
        <div class="i-hero-inner-border">
            
            
        </div>
    </section>

    <section class="i-home-cards i-home-section">
        <h2 class="i-home-cards-title">Things I've Made</h2>
        <div class="i-home-cards-grid">
            {{#get "posts" include="tags" filter="tags:things+featured:false" limit="6" as |sites| }}
                {{#foreach sites}}
                    {{> "picture-card"}}
                {{/foreach}}
            {{/get}}
        </div>
    </section>

    <section class="i-home-cards i-home-section i-home-cards-bg-2">
            <h2 class="i-home-cards-title">Web Dev &amp; Design</h2>
        <div class="i-home-cards-grid">
            {{#get "posts" include="tags" filter="tags:web-dev+tags:-css+featured:false" limit="6" as |sites| }}
                {{#foreach sites}}
                    {{> "picture-card"}}
                {{/foreach}}
            {{/get}}
        </div>
    </section>

    <section class="i-poodle i-home-section">
        <video autoplay loop preload muted width="1280">
        <source src="/assets/video/poodle.mp4"
            type="video/webm">
        </video>
        <div class="i-poodle-flex">
            <div class="i-poodle-top">
                <p>Here's the thing</p>
                <p>Here's the thing</p>
                <p>Here's the thing</p>
            </div>
            <div class="i-poodle-bottom">
                <p>Life's nothing without a poodle<br><em>and an ice cream cone</em></p>
            </div>
        </div>
    </section>

    <section class="i-home-cards i-home-section i-home-cards-bg-2">
            <h2 class="i-home-cards-title">Words on Words</h2>
        <div class="i-home-cards-grid">
            {{#get "posts" include="tags" filter="tags:words" limit="6" as |sites| }}
                {{#foreach sites}}
                    {{> "picture-card"}}
                {{/foreach}}
            {{/get}}
        </div>
    </section>

    <section class="i-home-section i-cta">
            <h2 class="i-home-cards-title">It's epistolary, bro</h2>
        <div class="flex-column">
            <div class="i-cta-text">
                <p>It's not a poodle or ice cream, but your life's better (or, more truthfullly, different) with my newsletter</p>
                <p>I mostly write about web dev &amp; design, but once in a while there's a recipe, etymology, or story thrown in for good measure</p>
            </div>
            <form class="i-cta-form" data-members-subscribe>
                <div class="flex-column">
                    
                    <input type="email" class="i-input" aria-label="email">
                    <span class="i-cta-icon">
                        <svg class="i-mail-icon" viewBox="0 0 24 24" aria-hidden="true"><path fill="none" d="M0 0h24v24H0z"/><path d="M3 3h18a1 1 0 0 1 1 1v16a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1zm17 4.238l-7.928 7.1L4 7.216V19h16V7.238zM4.511 5l7.55 6.662L19.502 5H4.511z"/></svg>
                    </span>
                </div>
                <button class="i-button">Subscribe</button>
            </form>
        </div>
    </section>

    <section class="i-home-cards i-home-section i-home-cards-bg-2">
            <h2 class="i-home-cards-title">For the Braniacs</h2>
        <div class="i-home-cards-grid">
            {{#get "posts" include="tags" filter="tags:academic" limit="6" as |sites| }}
                {{#foreach sites}}
                    {{> "line-card"}}
                {{/foreach}}
            {{/get}}
        </div>
    </section>
</main>

